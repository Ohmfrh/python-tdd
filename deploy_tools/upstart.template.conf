[unit]
Description=gunicorn daemon for tdd-staging
After=network.target

[Service]
User=rlogin
Group=www-data
WorkingDirectory=/home/rlogin/sites/python-tdd
ExecStart=/home/rlogin/.virtualenvs/tdd/bin/gunicorn --bind unix:/tmp/tdd-staging.danielcastro.tech superlists.wsgi:application

[Install]
WantedBy=multi-user.target
